# Level 31 -> 32

This time your task is to push a file to the remote repository.

Details:

    File name: key.txt
    
    Content: 'May I come in?'
    
    Branch: master
    
git commit key.txt

but it doesn't work because gitignore ignores all .txt files
So we fix that first.
