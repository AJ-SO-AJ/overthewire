# Level 14 -> 15

You need to send the current password of bandit14 to localhost:30000.

ssh is used to open a session/login.

nc (netcat) is used for a lot of things including sending messages.

nc host port (opens communication)

Once communication is open, you can send message. Sending the previous password gave me the next one.
